# Ki-ring World avatar layers

Every asset uses a transparent `200 × 200` canvas and is stacked at the same
origin. Keep filenames stable when replacing these placeholders with finished
SVG or PNG pixel art. The layer order is background, skin, clothes, face, hair,
then accessory.
